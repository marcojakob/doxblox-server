library text_answer_question_editor;

import 'dart:html';
import 'package:web_ui/web_ui.dart';

import '../../model/model.dart';

/**
 * Editor for [TextAnswerQuestion]s.
 */
class TextAnswerQuestionEditor extends WebComponent {
  
  TextAnswerQuestion question;
  
  /**
   * Lifecycle method invoked whenever a component is added to the DOM.
   */
  inserted() {
  }
}
